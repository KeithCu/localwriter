# Notes — Writer → Calc write (SAR / floorstand style)

Purpose: **harness debug** stub for the first eval-2 **two-turn peer-write**.
Writer is the chat start. The workbook must be **written** via the already-open
Calc sidebar / peer, not read as research-only.

Eval-2 is not a multi-model benchmark yet. This folder is a **stub**, not a
headed-ready gold.

## How it differs from existing siblings

| Sibling | Deliverable | Other file |
|---------|-------------|------------|
| Tenant / Cadaver | Writer memo / proposal | Folder refs; Cadaver budget is **research-only** |
| AFC | Calc in-workbook sampling | No Writer peer |
| GMP | Writer memo + Draw form fill | Peer exists, but Draw is a **side form** |
| **This stub** | Calc workbook is a **write** target | Writer starts; two caller turns |

Pre-open cheat (when materials land): open the Writer brief **and** the
Calc workbook before START. Open the **Calc sidebar once** so the workbook
is a live peer. Paste the Writer prompt in the Writer sidebar. Watch the
UNO message box and `writeragent_debug.log`.

Do **not** treat Cadaver Budget or AFC Population as this sibling. Those
already exist and are not Writer→Calc write.

## Product prompt rules

`prompt.writeragent.txt` must stay a gold-shaped claim. Do **not** name
product internals in that file. Soft oracles later; no KPI gaming in the
product prompt.

## Not ready

No in-repo gold package, no fixtures, no headed `--task`, no oracle CLI.
See [`SOURCE.md`](SOURCE.md).
