# Notes — Draw-primary deliverable

Purpose: **harness debug** stub where **Draw is the product** — org chart,
process map, or floorstand **layout**. Score `get_draw_tree` + shapes /
fill, not a Writer memo that happens to mention a diagram.

Eval-2 is not a multi-model benchmark yet. This folder is a **stub**, not a
headed-ready gold.

## How it differs from existing siblings

GMP Change Control pre-opens a Draw **form stand-in** so a Writer risk
memo can be filled. Draw is a side form. This slot inverts that: the
open Draw document **is** the deliverable. Writer is optional or absent.

Peer v1 rejects `PresentationDocument`. Do **not** use Impress as a
substitute (even if gold is a deck).

## Pre-open cheat (when materials land)

Open the Draw document (blank canvas or staged stand-in). Open the
**Draw sidebar once**. Paste the prompt there. Folder refs (PDF org
chart, FTE sheet) stay research-only unless the gold also asks for them.

TODO (harness, later): auto-open the Draw deck without a human click —
same note as GMP. Do not invent a second panel-factory here.

## Not ready

No in-repo gold package, no fixtures, no headed `--task`, no oracle CLI.
See [`SOURCE.md`](SOURCE.md).
